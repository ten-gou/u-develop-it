// views the available database/tables w/in the database
// SHOW DATABASES/TABLES;

// select a specific database
// USE <database>;

// Select a specific id from sql TABLE
// SELECT <x, y, z>
// FROM <table>
// WHERE <x/y/z> = <value>;

// generates a database/table using sql files
// SOURCE <path to the sql file>

// alters a preexisting table
// ALTER TABLE <table> ADD COLUMN <column name> INTEGER;

// imports data from one table to another
// left join, inner join, right join, full outer join

// deletes a table/database
// DROP DATABASE/TABLE <name of table/database>;

// tallies votes within the votes table
// SELECT COUNT(candidate_id) FROM votes GROUP BY candidate_id;
